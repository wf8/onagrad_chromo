
# mcmc

The simulator uses 40 different moves in a random move schedule with 190 moves per iteration

# runs

200 independent runs of 10000 iterations (1 sample per iteration),
each sampling a tree from the posterior distribution of trees.

The first 10% discarded as burnin.

